# Secrets (local only)
.env
backend/.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Large manuals (keep directory via .gitkeep)
manuals/**/*.pdf
manuals/**/*.PDF

# Processed / local vector data (keep directory via .gitkeep)
/data/*
!/data/.gitkeep

# Local pipeline scratch artifacts (channel-map crops/strips/renders)
backend/fixtures/pipeline/**/artifacts/channel_map_*

# Migration-generated location reports (byproduct of 020)
backend/reports/
