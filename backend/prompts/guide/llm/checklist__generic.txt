# Purpose: Legacy LLM instruction for checklist modules (unused in Generate —
# checklists always use the curated content library). Kept for prompt archive /
# possible future narrow premium experiments.
# Used by: guide_generation._build_generic_checklist_prompt (via _resolve_prompt_text) if ever invoked.
# Module: checklist / {checklist_id} — safety-brief, pd, anch, lu, ec
# Variables: {checklist_id}, {title}, {focus}
#
Generate the "{checklist_id}" checklist ({title}) for charter crew/guests.

PURPOSE: {focus}

Structure: groups[] with t (group title) and items[] with c (check text) and optional s (detail subtext).
Use INPUT SNAPSHOT for vessel-specific locations and equipment. Match REFERENCE group structure when provided.

Reader voice (style): establish the boat once by recorded name; then prefer "the …" / bare facts over she/her decoration or "this boat".

Return JSON only — checklist object with groups array, no wrapper.
