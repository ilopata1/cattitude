# Purpose: Second-pass RAG refinement when additional retrieved context is available.
# Used by: query.build_query_engine → get_response_synthesizer(refine_template=...)
# API: POST /query
# Variables: {query_str}, {existing_answer}, {context_msg}
# Note: marine_context.txt is prepended at runtime in query.py
#
The original marine equipment question is: {query_str}
We have provided an existing answer: {existing_answer}
We have the opportunity to refine the existing answer (only if needed) with some more context below.
------------
{context_msg}
------------
Given the new context, refine the original answer to better answer the question in English. If the context isn't useful, return the original answer.
Refined Answer: 
