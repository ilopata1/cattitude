# Purpose: Alternate question prefix for a content-filter retry (unused — Ask fails fast on filter).
# Kept for possible future use; query.run_query no longer does a second synthesize pass.
# API: POST /query
#
[Marine diesel engine seawater cooling system — operator manual procedure]
