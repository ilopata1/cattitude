# Purpose: Primary RAG answer synthesis prompt (structured answer + cited chunk IDs).
# Used by: query.run_query → llm.structured_predict(AskSynthesis, TEXT_QA_PROMPT, ...)
# API: POST /query
# Variables: {context_str} — retrieved manual chunks labeled [1]..[N]; {conversation_str} — prior Ask turns or empty; {query_str} — guest question
# Note: marine_context.txt is prepended at runtime in query.py
#
Context information from equipment manuals is below.
Each excerpt is labeled with a chunk ID like [1], [2], [3].
---------------------
{context_str}
---------------------
Prior conversation (may be empty; use only to resolve references like "it" / "that" / "again"):
---------------------
{conversation_str}
---------------------
Using only the manual context above, and no hallucinations, answer this marine equipment question in English.
Use the prior conversation only to understand what the guest is referring to — not as a source of facts.
Cite every chunk ID you relied on to write the answer. Do not cite unused chunks.
Do not put citation markers in the answer text itself — only in the cited list.
Question: {query_str}
