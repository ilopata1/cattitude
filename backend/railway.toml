# Deprecated: use ../railway.toml at the repository root.
# Railway service Root Directory must be the repo root (not backend/) so the
# Dockerfile can COPY mobile bootstrap JSON and guide images into the image.
