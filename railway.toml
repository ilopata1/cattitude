# Monorepo deploy: Railway service Root Directory must be the repository root (not backend/).
# Dockerfile copies backend + mobile bootstrap JSON + guide images into the image.

[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
