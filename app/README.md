# Legacy PWA (archived)

This single-file PWA is **no longer deployed**. Production is the Ionic app in [`mobile/`](../mobile/) at https://ilopata1.github.io/cattitude/

Content is still extracted from `index.html` via `utilities/extract_bootstrap_content.mjs` until bootstrap JSON becomes the source of truth.
